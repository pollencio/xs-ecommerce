# --- Sanity (CMS) ---
# Project settings: https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-11-01

# Only needed if the dataset is private (not recommended for this boilerplate).
# SANITY_API_READ_TOKEN=

# --- Site ---
# Used to build absolute URLs for SEO (canonical, sitemap, OG, JSON-LD).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
